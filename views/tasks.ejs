<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Tasks</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
    
    <style>
        body {
            font-family: "Open Sans", sans-serif;
            font-optical-sizing: auto;
            margin: 0;
            padding: 0;
            background-color: rgb(14, 14, 46);
        }
        .container {
            max-width: 900px;
            width: 90%;
            background-color: #fff;
            padding: 20px;
            margin: 30px auto;
            border-radius: 12px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }
        h1, h2 {
            text-align: center;
            color: #333;
            margin-top: 0;
        }
        form {
            margin-bottom: 30px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #333;
        }
        input, textarea, select {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 6px;
            font-size: 16px;
        }
        .buttons {
            min-width: 75%;
            background-color: #fff;
            display: flex;
            margin-left: 10%;
            margin-right: 10%;
            align-items: center;
            gap: 12px;
            justify-content: space-between;
            padding-left: 12px;
            padding-right: 12px;
            border: 1px solid white;
            border-radius: 21px;
            margin-top: 9px;
        }
        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 16px;
        }
        button:hover {
            background-color: #0056b3;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            padding: 12px;
            text-align: left;
            border: 1px solid #ddd;
        }
        th {
            background-color: rgb(14, 14, 46);
            color: white;
        }
        a {
            color: #007bff;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
        .btn-primary {
            background-color: white;
            text-align: center;
            padding: 10px;
            color: #0056b3;
            text-decoration: none;
            border-radius: 6px;
            margin-top: 15px;
            margin-bottom: 12px;
            display: inline-block;
            font-weight: 600;
            border: 1px solid #0056b3;
        }
        .btn-primary:hover{
            text-decoration: none;
            background-color: #0056b3;
            color: white;
        }
        .task-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .task-header h1 {
            color: black;
        }
        .acts {
            display: flex;
            gap: 4px;
        }

        /* Search Bar Styles */
        .search-form {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }
        .search-form input[type="text"] {
            flex: 1; /* Make input flexible to fill available space */
            padding: 10px 15px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 6px 0 0 6px;
            box-sizing: border-box;
        }
        .search-button {
            width: 50px; /* Fixed width for the button */
            padding: 10px 0; /* Adjust padding for better aesthetics */
            background: #007bff;
            border: none;
            cursor: pointer;
            border-radius: 0 6px 6px 0;
            display: flex;
            align-items: center;
            justify-content: center; /* Center the icon */
        }
        .search-button img {
            width: 24px; /* Set a fixed size for the icon */
            height: 24px;
        }
        .logout{
            background: white;
            color: rgb(231, 54, 54);
            text-decoration: none;
            border: 1px solid rgb(231, 54, 54);
            padding: 10px;
            font-weight: 600;
            border-radius: 20px;
            margin-bottom: 5px;
        }
        
        .logout:hover{
            background: rgb(231, 54, 54);
            color: white;
            text-decoration: none;
        }
        .new{
            color: #0056b3;
            text-decoration: none;
            font-weight: 600;
            padding: 9px;
            border: 1px solid #0056b3;
            border-radius: 20px;
        }
        .new:hover{
            background: #0056b3;
            color: white;
            text-decoration: none;
        }
        .imgm{
            height: 55px;
        }
        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }
            .task-header {
                flex-direction: column;
                align-items: flex-start;
            }
            .search-form {
                flex-direction: row; /* Keep elements in one line */
                width: 100%;
            }
            .search-form input[type="text"] {
                margin-right: 5px; /* Space between input and button */
            }
            table, thead, tbody, th, td, tr {
                display: block;
            }
            tr {
                margin-bottom: 10px;
                border-bottom: 1px solid #ddd;
            }
            td {
                display: flex;
                justify-content: space-between;
                padding: 10px;
                text-align: left;
                position: relative;
                font-weight: 300;
            }
            td::before {
                content: attr(data-label);
                font-weight: bold;
                flex: 1;
            }
            th {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="buttons">
        <a href="/tasks/create" class="new">Create New Task</a> 
       <img src="https://vahanhelp.in/images/2DPNG.png" class="imgm">
        <a href="/logout" class="logout" style="display: block; text-align: center; margin-top: 20px;"><i class="fas fa-arrow-left"></i>Logout</a>
    </div>
    
    <div class="container">
        <h2>Search Tasks By Car Number</h2>
        <form action="/tasks/search" method="get" class="search-form">
            <input type="text" id="searchCarNum" name="carNum" placeholder="Enter car number" required>
            <button type="submit" class="search-button">
                <img src="https://img.icons8.com/ios-filled/50/ffffff/search.png" alt="Search">
            </button>
        </form>      

        <div class="task-header">
            <h1>Your Tasks</h1>
            <a href="/tasks/download" class="btn-primary">Download all</a>
        </div>

        <% if (tasks.length > 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>Car Number</th>
                        <th>Task type</th>
                        <th>Case type</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% tasks.forEach(task => { %>
                        <tr>
                            <td data-label="Car Number"><%= task.carNum %></td>
                            <td data-label="Task Name"><%= task.name %></td>
                            <td data-label="Case type"><%= task.caseType %></td>
                            <td data-label="Status"><%= task.state ? 'Completed' : 'Pending' %></td>
                            <td data-label="Actions" class="acts">
                                <a href="/tasks/view/<%= task._id %>" class="btn-view" title="View">
                                    <button type="button" style="padding: 5px 10px; background-color: green; color: white; border: none; border-radius: 4px; cursor: pointer;">
                                        View
                                    </button>
                                </a>
                                <a href="/tasks/edit/<%= task._id %>" title="Edit">
                                    <button type="button" style="padding: 5px 10px; background-color: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer;">
                                        Edit
                                    </button>
                                </a>
                                <form action="/tasks/delete/<%= task._id %>" method="post" style="display:inline;">
                                    <button type="submit" onclick="return confirm('Are you sure you want to delete this task?');" title="Delete" style="padding: 5px 10px; background-color: #ff0000; color: white; border: none; border-radius: 4px; cursor: pointer;">
                                        Delete
                                    </button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p>No tasks found.</p>
        <% } %>
    </div>
</body>
</html>
