<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Task</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.9.3/html2pdf.bundle.min.js"></script>
   <style>
        body {
            font-family: 'Roboto', Arial, sans-serif;
            background-color: #f4f7f6;
            margin: 0;
            padding: 20px;
            font-family: "Open Sans", sans-serif;
            font-optical-sizing: auto;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 40px;
            background: linear-gradient(145deg, #ffffff, #e6e8ea);
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            border-top: 5px solid #0d6efd;
            transition: all 0.3s ease-in-out;
        }
        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
            font-size: 26px;
            font-weight: 700;
        }
        .task-details {
            display: flex;
            flex-direction: column;
            gap: 10px;
            padding: 20px;
            border-radius: 10px;
            background: #f9fbfd;
            box-shadow: inset 0 4px 8px rgba(0, 0, 0, 0.05);
        }
        .task-details p {
            display: flex;
            justify-content: flex-start;
            align-items: center;
            font-size: 16px;
            line-height: 1.4;
            color: #555;
            margin: 5px;
            padding: 8px;
            border-bottom: 1px solid #ececec;
            background: #fff;
            border-radius: 5px;
            transition: transform 0.2s ease-in-out;
        }
        .task-details p strong {
            flex: 0 0 420px; /* Ensures the label is aligned to the left */
            text-align: left;
            color: #0d6efd;
        }
        .task-details p span {
            flex: 1;
            text-align: left;
        }
        .task-details p:hover {
            transform: translateX(5px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
        }
        .back-link {
            text-decoration: none;
            color: white;
            background-color: #007bff;
            padding: 10px;
            transition: background-color 0.3s;
            border: 1px solid transparent;
            border-radius: 50%;
        }
        .back-link:hover {
            background-color: #084298;
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.2);
        }
        .back-link .icon {
            margin-right: 8px;
            vertical-align: middle;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                padding: 20px;
                margin: 10px;
            }
            h1 {
                font-size: 22px;
                margin-bottom: 20px;
            }
            .task-details p {
                font-size: 14px;
                padding: 6px;
            }
            .task-details p strong {
                flex: 0 0 120px; /* Reduce width for smaller screens */
            }
        }
        .download-btn {
    background-color: #0d6efd;
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
    margin-top: 20px;
}

.download-btn:hover {
    background-color: #084298;
}
.biten{
    display: flex;
    align-items: center;
    justify-content: space-between;
}

        @media (max-width: 480px) {
            .container {
                padding: 15px;
                margin: 5px;
            }
            h1 {
                font-size: 20px;
                margin-bottom: 15px;
            }
            .task-details p {
                font-size: 13px;
                padding: 5px;
            }
            .task-details p strong {
                flex: 0 0 110px; /* Further reduce width on mobile */
            }
            .back-link {
                padding: 8px;
            }
        }
    </style>
</head>
<body>
    <div class="biten">
        <a href="/tasks/" class="back-link">
            <i class="fas fa-arrow-left"></i>
          </a>
          <button id="download-pdf" class="download-btn">Download as PDF</button>
    
    </div>
    <div class="container">
        <h1>Task Details</h1>
        <div class="task-details">
            
            <p><strong>Client name:</strong> <span><%= task.clientName %></span></p>
            <p><strong>File received date:</strong> <span><%= task.fileReceivedDate ? task.fileReceivedDate.toDateString() : 'N/A' %></span></p>
            <p><strong>Task type:</strong> <span><%= task.name %></span></p>
            <p><strong>Car Number:</strong> <span><%= task.carNum %></span></p>
            <p><strong>Case type:</strong> <span><%= task.caseType %></span></p>
            <p><strong>Additional work:</strong> <span><%= task.AdditionalWork %></span></p>
            <p><strong>HPT name:</strong> <span><%= task.hptName %></span></p>
            <p><strong>HPA:</strong> <span><%= task.HPA %></span></p>
            <p><strong>RC status:</strong> <span><%= task.status_RC %></span></p>
            <p><strong>NOC status:</strong> <span><%= task.status_NOC %></span></p>
            <p><strong>Chesis number:</strong> <span><%= task.chesisnum %></span></p>
            <p><strong>Engine number:</strong> <span><%= task.engineNum %></span></p>
            <p><strong>Seller Name:</strong> <span><%= task.sellerName %></span></p>
            <p><strong>Seller Number:</strong> <span><%= task.sellerNum %></span></p>
            <p><strong>Seller RTO Location:</strong> <span><%= task.seller_RTO_location %></span></p>
            <p><strong>Seller Aligned Date:</strong> <span><%= task.sellerAlignedDate ? task.sellerAlignedDate.toDateString() : 'N/A' %></span></p>
            <p><strong>Buyer Name:</strong> <span><%= task.buyerName %></span></p>
            <p><strong>Buyer Number:</strong> <span><%= task.buyerNum %></span></p>
            <p><strong>Buyer RTO Location:</strong> <span><%= task.buyer_RTO_location %></span></p>
            <p><strong>Buyer Aligned Date:</strong> <span><%= task.buyerAlignedDate ? task.buyerAlignedDate.toDateString() : 'N/A' %></span></p>
            <p><strong>NOC issued Date:</strong> <span><%= task.NOCissuedDate ? task.NOCissuedDate.toDateString() : 'N/A' %></span></p>
            <p><strong>Transfer Date:</strong> <span><%= task.transferDate ? task.transferDate.toDateString() : 'N/A' %></span></p>
            <p><strong>RC handover Date:</strong> <span><%= task.HandoverDate_RC ? task.HandoverDate_RC.toDateString() : 'N/A' %></span></p>
            <p><strong>NOC handover Date:</strong> <span><%= task.HandoverDate_NOC ? task.HandoverDate_NOC.toDateString() : 'N/A' %></span></p>       
            <p><strong>Deliver Date:</strong> <span><%= task. deliverdate ? task. deliverdate.toDateString() : 'N/A' %></span></p>
            <p><strong>Courier done Date:</strong> <span><%= task.courier ? task.courier.toDateString() : 'N/A' %></span></p>
            <p><strong>Remarks:</strong> <span><%= task.description %></span></p>   
            <p><strong>Status:</strong> <span><%= task && task.state %></span></p>
            <p><strong>Seller Photo:</strong> <span>
                <% if (task.sellerPhoto) { %>
                    <a href="<%= task.sellerPhoto %>" target="_blank">View Photo</a>
                <% } else { %>
                    N/A
                <% } %>
            </span></p>
            <p><strong>Buyer Photo:</strong> <span>
                <% if (task.buyerPhoto) { %>
                    <a href="<%= task.buyerPhoto %>" target="_blank">View Photo</a>
                <% } else { %>
                    N/A
                <% } %>
            </span></p>
            <p><strong>Seller Docs:</strong> <span>
                <% if (task.sellerDocs) { %>
                    <a href="<%= task.sellerDocs %>" target="_blank">View Document</a>
                <% } else { %>
                    N/A
                <% } %>
            </span></p>
            <p><strong>Buyer Docs:</strong> <span>
                <% if (task.buyerDocs) { %>
                    <a href="<%= task.buyerDocs %>" target="_blank">View Document</a>
                <% } else { %>
                    N/A
                <% } %>
            </span></p>
            <p><strong>Car Video:</strong> <span>
                <% if (task.carVideo) { %>
                    <a href="<%= task.carVideo %>" target="_blank">View Video</a>
                <% } else { %>
                    N/A
                <% } %>
            </span></p>
            <p><strong>Seller Video:</strong> <span>
                <% if (task.sellerVideo) { %>
                    <a href="<%= task.sellerVideo %>" target="_blank">View Video</a>
                <% } else { %>
                    N/A
                <% } %>
            </span></p>
            <p><strong>Care Of Video:</strong> <span>
                <% if (task.careOfVideo) { %>
                    <a href="<%= task.careOfVideo %>" target="_blank">View Video</a>
                <% } else { %>
                    N/A
                <% } %>
            </span></p>
            <p><strong>NOC Receipt:</strong> <span>
                <% if (task.nocReceipt) { %>
                    <a href="<%= task.nocReceipt %>" target="_blank">View Receipt</a>
                <% } else { %>
                    N/A
                <% } %>
            </span></p>
            <p><strong>Transfer Receipt:</strong> <span>
                <% if (task.transferReceipt) { %>
                    <a href="<%= task.transferReceipt %>" target="_blank">View Receipt</a>
                <% } else { %>
                    N/A
                <% } %>
            </span></p>
        </div>
    </div>
    <script>
        document.getElementById('download-pdf').addEventListener('click', function() {
            var element = document.querySelector('.container');
            html2pdf()
                .set({
                    margin: 0.1,
                    filename: 'task-details.pdf',
                    image: { type: 'jpeg', quality: 1},
                    html2canvas: { scale: 2 },
                    jsPDF: { unit: 'in', format: 'letter', orientation: 'portrait' }
                })
                .from(element)
                .save();
        });
    </script>
</body>
</html>
