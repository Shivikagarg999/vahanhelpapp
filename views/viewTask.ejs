<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Task</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', Arial, sans-serif;
            background-color: #f4f7f6;
            margin: 0;
            padding: 20px;
            font-family: "Open Sans", sans-serif;
            font-optical-sizing: auto;
        }
        .container {
            max-width: 800px;
            margin: 60px auto;
            padding: 40px;
            background: linear-gradient(145deg, #ffffff, #e6e8ea);
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            border-top: 5px solid #0d6efd;
            transition: all 0.3s ease-in-out;
        }
        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 40px;
            font-size: 30px;
            font-weight: 700;
        }
        .task-details {
            display: flex;
            flex-direction: column;
            gap: 20px;
            padding: 20px;
            border-radius: 10px;
            background: #f9fbfd;
            box-shadow: inset 0 4px 8px rgba(0, 0, 0, 0.05);
        }
        .task-details p {
            display: flex;
            justify-content: space-between;
            font-size: 18px;
            line-height: 1.6;
            color: #555;
            margin: 0;
            padding: 10px;
            border-bottom: 1px solid #ececec;
            background: #fff;
            border-radius: 5px;
            transition: transform 0.2s ease-in-out;
        }
        .task-details p:hover {
            transform: translateX(5px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
        }
        .task-details p strong {
            color: #0d6efd;
        }
        .back-link {
            text-decoration: none;
    color:  white;
    background-color: #007bff;
    padding: 10px;
    transition: background-color 0.3s;
    border: 1px solid transparent;
    border-radius: 50%;
        }
        .back-link:hover {
            background-color: #084298;
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.2);
        }
        .back-link .icon {
            margin-right: 8px;
            vertical-align: middle;
        }
    </style>
</head>
<body>
    <a href="/tasks/" class="back-link">
        <i class="fas fa-arrow-left"></i>
      </a>
    <div class="container">
        <h1>Task Details</h1>
        <div class="task-details">
            <p><strong>Name:</strong> <span><%= task.name %></span></p>
            <p><strong>Description:</strong> <span><%= task.description %></span></p>
            <p><strong>Car Number:</strong> <span><%= task.carNum %></span></p>
            <p><strong>Seller Name:</strong> <span><%= task.sellerName %></span></p>
            <p><strong>Seller Number:</strong> <span><%= task.sellerNum %></span></p>
            <p><strong>Buyer Name:</strong> <span><%= task.buyerName %></span></p>
            <p><strong>Buyer Number:</strong> <span><%= task.buyerNum %></span></p>
            <p><strong>Aligned Date:</strong> <span><%= task.align_date ? task.align_date.toDateString() : 'N/A' %></span></p>
            <p><strong>Seller RTO Location:</strong> <span><%= task.seller_RTO_location %></span></p>
            <p><strong>Buyer RTO Location:</strong> <span><%= task.buyer_RTO_location %></span></p>
            <p><strong>Status:</strong> <span><%= task && task.state ? 'Completed' : 'Pending' %></span></p>
        </div>
        <!-- <a class="back-link" href="/tasks"><span class="icon">&#8592;</span> Back to Tasks</a> -->
    </div>
</body>
</html>
