<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vahan Help - Login</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            margin: 0;
            padding: 0;               
            background: #f4f6f9; /* Light gray background */
            color: #333;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1em 2em;
            background: #111111; /* White background for header */
            color: #ffffff; /* Dark text */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .header img {
            max-height: 60px;
        }
        .header nav {
            display: flex;
            gap: 1em;
        }
        .header a {
            text-decoration: none;
            color: #ffffff; /* Professional blue for links */
            font-weight: 500;
            padding: 0.5em 1em;
            border-radius: 5px;
            transition: background 0.3s, color 0.3s;
            border: 1px solid transparent;
        }
        .header a:hover {
            background: #007bff; /* Blue background on hover */
            color: #ffffff; /* White text on hover */
            border-color: #007bff;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            flex: 1;
            padding: 2em;
            max-width: 500px;
            margin: auto;
        }
        .login-form {
            background: #ffffff; /* White background for form */
            padding: 2em;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px; /* Ensure form width is responsive */
        }
        .login-form h2 {
            margin-top: 0;
            font-size: 2em;
            color: #333;
        }
        .login-form label {
            display: block;
            font-size: 14px;
            color: #555;
            margin: 0.5em 0 0.2em;
        }
        .login-form input {
            width: 100%;
            padding: 10px;
            margin: 0.5em 0 1em;
            border-radius: 5px;
            border: 1px solid #ddd;
            font-size: 16px;
        }
        .login-form button {
            width: 100%;
            padding: 10px;
            background-color: #007bff; /* Blue button color */
            color: #ffffff;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background 0.3s;
        }
        .login-form button:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }
        .footer {
            text-align: center;
            padding: 1em;
            color: #007bff; /* White background for footer */
            background-color: #fff;
            box-shadow: 0 -4px 8px rgba(0, 0, 0, 0.1);
            font-weight: bold;
        }
        .footer a {
            color: #007bff; /* Professional blue for links */
            text-decoration: none;
            
        }
        .footer a:hover {
            text-decoration: underline;
        }
        .err{
            text-align: center;
            color: red;
        }
        @media (max-width: 768px) {
            .header {
                padding: 1em;
            }
            
            .header h1{
                font-size: 20px;
            }
            .header nav {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5em;
            }
            .header a {
                padding: 0.5em;
                font-size: 14px;
            }
            .login-form h2 {
                font-size: 1.5em;
            }
        }
        @media (max-width: 480px) {
            .header img {
                max-height: 50px;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="https://vahanhelp.in/images/2DPNG.png" alt="Vahan Help Logo">
        <h1>Partner's Login</h1>
        <nav>
            <a href="/">Home</a>
            <!-- <a href="/register">Sign Up</a> -->
        </nav>
    </div>
    <div class="container">
        <form class="login-form" action="/login" method="POST">
            <h2>Login</h2>
            <label for="company">Company Name</label>
            <input type="text" id="company" name="company" placeholder="Enter your company name" required>
            
            <label for="password">Password</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" required>
            
            <button type="submit">Login</button>
        </form>
     
    </div>
    <% if (typeof error !== 'undefined' && error) { %>
        <p class="err"><%= error %></p>
    <% } %>
    <div class="footer">
        <p>&copy; 2024 Vahan Help CMS.</p>
    </div>
</body>
</html>
 