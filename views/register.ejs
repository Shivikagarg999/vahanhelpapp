<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vahan Help - Register</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #f4f6f9; /* Light gray background */
            color: #333;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1em 2em;
            background: #ffffff; /* White background for header */
            color: #333; /* Dark text */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .header img {
            max-height: 60px;
        }
        .header nav {
            display: flex;
            gap: 1em;
        }
        .header a {
            text-decoration: none;
            color: #007bff; /* Professional blue for links */
            font-weight: 500;
            padding: 0.5em 1em;
            border-radius: 5px;
            transition: background 0.3s, color 0.3s;
            border: 1px solid transparent;
        }
        .header a:hover {
            background: #007bff; /* Blue background on hover */
            color: #ffffff; /* White text on hover */
            border-color: #007bff;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            flex: 1;
            padding: 2em;
            max-width: 600px; /* Increased max-width for better form size */
            margin: auto;
        }
        .register-form {
            background: #ffffff; /* White background for form */
            padding: 2em;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px; /* Ensure form width is responsive */
        }
        .register-form h2 {
            margin-top: 0;
            font-size: 2em;
            color: #333;
        }
        .register-form label {
            display: block;
            font-size: 14px;
            color: #555;
            margin: 0.5em 0 0.2em;
        }
        .register-form input {
            width: 100%;
            padding: 10px;
            margin: 0.5em 0 1em;
            border-radius: 5px;
            border: 1px solid #ddd;
            font-size: 16px;
        }
        .register-form button {
            width: 100%;
            padding: 10px;
            background-color: #007bff; /* Blue button color */
            color: #ffffff;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background 0.3s;
        }
        .register-form button:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }
        .footer {
            text-align: center;
            padding: 1em;
            background: #ffffff; /* White background for footer */
            color: #333;
            box-shadow: 0 -4px 8px rgba(0, 0, 0, 0.1);
        }
        .footer a {
            color: #007bff; /* Professional blue for links */
            text-decoration: none;
        }
        .footer a:hover {
            text-decoration: underline;
        }
        @media (max-width: 768px) {
            .header {
                padding: 1em;
            }
            .header nav {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5em;
            }
            .header a {
                padding: 0.5em;
                font-size: 14px;
            }
        }
        @media (max-width: 480px) {
            .header img {
                max-height: 50px;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="https://vahanhelp.in/images/2DPNG.png" alt="Vahan Help Logo"> <!-- Replace with your logo URL -->
        <nav>
            <a href="/">Home</a>
            <a href="/login">Login</a>
        </nav>
    </div>
    <div class="container">
        <form class="register-form" action="/register" method="POST">
            <h2>Register</h2>
            <label for="company">Company Name</label>
            <input type="text" id="company" name="company" placeholder="Enter your company name" required>
            
            <label for="password">Password</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" required>
            
            <button type="submit">Register</button>
        </form>
    </div>
    <div class="footer">
        <p>&copy; 2024 Vahan Help. <a href="/login">Already have an account? Login</a></p>
    </div>
</body>
</html>
