<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vahan Help - Home</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
      
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background: #f4f6f9;
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100vh;
            max-height: 100vh;
            font-family: "Open Sans", sans-serif;
            font-optical-sizing: auto;
            position: relative;
        }
        .logo {
            margin-top: 20px; 
            z-index: 2;
        }
        .container {
            text-align: center;
            position: relative;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            gap: 323px;
            z-index: 2;
            color: white;
            width: 100%; /* Ensure the container takes full width */
        }
        .container h1 {
            font-size: 3rem;
            margin: 0; /* Remove bottom margin */
            font-weight: bold;
            letter-spacing: 2px;
            position: absolute;
            top: 60px;
        }
        .container .btn {
            padding: 1rem 2rem;
            background-color: #ffffff;
            color: black;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            font-size: 1.2rem;
            font-weight: bold;
            position: absolute;
            bottom: 60px;
            left: 50%;
            transform: translateX(-50%);
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
           box-shadow: 0 4px 6px rgba(0, 123, 255, 0.3);
            margin-top: 40px; 
            cursor: pointer;
        }
        .container .btn:hover {
            background-color: #0056b3;
            box-shadow: 0 6px 10px rgba(0, 123, 255, 0.4);
            color: #ffffff;
        }
        .background {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://i.imgur.com/ry7c92l.png') no-repeat center center;
            background-size: cover;
        }
        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            /* background: rgba(0, 0, 0, 0.7); */
            z-index: 1;
        }
        @media (max-width: 768px) {
            .container{
                gap: 400px;
            }
            .container h1 {
                font-size: 2.5rem;
            }
            .container .btn {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="overlay"></div>
    <div class="background"></div>
    <div class="container">
        <h1>Welcome to Vahan Help</h1>
        <a href="/login" class="btn">Access Your CMS</a>
    </div>
</body>
</html>
