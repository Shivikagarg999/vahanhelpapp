<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vahan Help - Home</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #f4f6f9; /* Light gray background */
            color: #333;
            display: flex;
            flex-direction: column;
            height: 100vh;
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1em 2em;
            background: black; /* White background for header */
            color: white; /* Dark text */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .header img {
            max-height: 60px;
        }
        .header nav {
            display: flex;
            gap: 1em;
        }
        .header a {
            text-decoration: none;
            color: #ffffff;
            font-weight: 500;
            padding: 0.5em 1em;
            border-radius: 5px;
            transition: background 0.3s, color 0.3s;
            border: 1px solid transparent;
        }
        .header a:hover {
            background: #007bff; /* Blue background on hover */
            color: #ffffff; /* White text on hover */
            border-color: #007bff;
        }
        .hero {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            flex: 1;
            text-align: center;
            background: url('/path-to-your-background-image.jpg') no-repeat center center;
            background-size: cover;
            color: black; /* Ensure text is readable against background */
            padding: 3em;
        }
        .hero h1 {
            font-size: 3em;
            margin: 0;
            font-weight: 700;
        }
        .hero p {
            font-size: 1.2em;
            margin: 0.5em 0;
        }
        .hero .btn {
            display: inline-block;
            padding: 0.75em 1.5em;
            margin: 0.5em;
            font-size: 1em;
            color: #ffffff;
            background: #007bff; /* Blue button color */
            border: none;
            border-radius: 5px;
            text-decoration: none;
            transition: background 0.3s;
        }
        .hero .btn:hover {
            background: #0056b3; /* Darker blue on hover */
        }
        @media (max-width: 768px) {
            .header {
                align-items: center;
            }
            .header h1{
                font-size: 20px;
            }
            .header nav {
                flex-direction: column;
                gap: 0.5em;
            }
            .hero {
                padding: 2em;
            }
            .hero h1 {
                font-size: 2.5em;
            }
            .hero p {
                font-size: 1.1em;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="https://vahanhelp.in/images/2DPNG.png" alt="Vahan Help Logo"> <!-- Replace with your logo URL -->
        <h1>Partner's Login</h1>
        <nav>
            <a href="/login">Login</a>
            <a href="/register">Sign Up</a>
        </nav>
    </div>
    <div class="hero">
        <h1>Welcome to Vahan Help</h1>
        <p>Your trusted task manager app</p>
        <!-- <a href="/login" class="btn">Login</a>
        <a href="/register" class="btn">Sign Up</a> -->
    </div>
</body>
</html>
